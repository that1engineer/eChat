# eChat
This is a simple chat application I made that lets allows multiple users to send messages to each other from separate devices. It runs through the terminal and requires telnet to be installed (which is a very small package you can download from a single command).
